import { AbstractOperation } from './operation';
import type { Callback } from '../utils';
import type { Collection } from '../collection';
import type { BulkWriteResult, BulkWriteOptions, AnyBulkWriteOperation } from '../bulk/common';
import type { Server } from '../sdam/server';
import type { ClientSession } from '../sessions';
/** @internal */
export declare class BulkWriteOperation extends AbstractOperation<BulkWriteResult> {
    options: BulkWriteOptions;
    collection: Collection;
    operations: AnyBulkWriteOperation[];
    constructor(collection: Collection, operations: AnyBulkWriteOperation[], options: BulkWriteOptions);
    execute(server: Server, session: ClientSession, callback: Callback<BulkWriteResult>): void;
}
//# sourceMappingURL=bulk_write.d.ts.map